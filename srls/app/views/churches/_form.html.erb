<%= form_for(@church) do |f| %>
  <% if @church.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@church.errors.count, "error") %> prohibited this church from being saved:</h2>

      <ul>
      <% @church.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<CENTER>
  <div class="field">
    <%= f.label :nombre, "Nombre de Parroquia" %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :vicaria, "Vicaria a la que Pertenece" %><br />
    <%= f.text_field :vicaria %>
  </div>
  <div class="actions">
    <%= f.submit "Crear Parroquia"%>
  </div>
<% end %>
</CENTER>